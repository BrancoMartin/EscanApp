# Dependencias de RUNTIME de EscanApp.
# Versiones fijadas: un producto comercial tiene que construirse igual hoy que
# en seis meses. Las dependencias que solo hacen falta para armar el release
# estan en requirements-build.txt.

fastapi==0.136.1
uvicorn==0.47.0
sqlalchemy==2.0.49
pydantic==2.13.4
python-dotenv==1.2.2
pywebview==6.2.1

# Agente de IA. Faltaban en el requirements original: un build limpio en otra
# maquina moria con "ModuleNotFoundError: No module named 'langchain_ollama'".
langchain==1.3.2
langchain-ollama==1.1.0

# Importacion masiva de productos desde Excel (.xlsx). El CSV se lee con la
# biblioteca estandar; openpyxl es lo unico que hace falta sumar para .xlsx.
openpyxl==3.1.5

# Necesario para recibir el archivo (multipart/form-data) en POST /products/import.
python-multipart==0.0.20
